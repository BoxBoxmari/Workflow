name: CI

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  lint-test-types:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt -r requirements-dev.txt

      - name: Install xvfb (headless GUI for tests)
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb

      - name: Flake8
        run: python -m flake8 .

      - name: Ruff
        run: python -m ruff check .

      - name: Pytest
        run: xvfb-run -a python -m pytest
        env:
          PYTHONPATH: ${{ github.workspace }}

      - name: UI smoke flake check (3x)
        run: |
          for i in 1 2 3; do
            echo "UI smoke run $i/3"
            xvfb-run -a python -m pytest -q tests/test_ui_smoke.py tests/ui
          done
        env:
          PYTHONPATH: ${{ github.workspace }}

      - name: Mypy
        run: python -m mypy .
